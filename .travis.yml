language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t douglasthoms/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true douglasthoms/docker-react npm run test
